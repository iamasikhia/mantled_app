import 'package:flutter/material.dart';

/// Global key which will be used to provide perform navigations in app
/// and also to perform business logic activities that need access to
/// [BuildContext]
GlobalKey<NavigatorState> navigatorKey = GlobalKey<NavigatorState>();
